# Project Context

Sample App is a small web application used to demonstrate AI Code Manager's workflow.

The coding agent should:

- Inspect the target repo before editing.
- Reuse existing application patterns.
- Add or update focused tests when implementing behavior.
- Write the required task result JSON after each run.
